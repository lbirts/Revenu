# Revenu

A dark-themed revenue analytics dashboard. Browse overview metrics, revenue trends, reports, and settings — with interactive charts and a collapsible sidebar.

## Getting started

```bash
npm install
npm run dev
```

Then open the local URL Vite prints in the terminal (usually `http://localhost:5173`).
